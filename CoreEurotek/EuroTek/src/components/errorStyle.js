import React from "react";
import { StyleSheet } from "react-native";

export const errInputStyle = StyleSheet.create({
    borderColor: "rgba(255, 0, 0, 0.75)",
    borderWidth: 3,
    color: "rgba(255, 0, 0, 0.75)",
})

export const errLabelStyle = StyleSheet.create({
    color: "rgba(255, 0, 0, 0.75)",
})